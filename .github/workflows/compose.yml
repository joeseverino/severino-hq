name: Compose and deploy extensions

# Builds the one image production runs: this host plus every admitted
# extension. Extensions verify and admit themselves, then trigger this; they no
# longer build or deploy an image of their own. That is the point -- when each
# extension shipped its own image, deploying one replaced the others.
#
# Triggered by an extension after a successful admission, or by hand to
# rebuild the current set (for example after a host release).

on:
  # After the host image exists, never in parallel with it being built. On a
  # plain push this raced the build and resolved a tag that was not published
  # yet.
  #
  # No branch filter, deliberately. This used to be `branches: [main]`, so the
  # composition -- the only place this application is assembled the way
  # production assembles it, DEBUG off and the extensions loaded -- was first
  # built from a commit that had already been merged. Three failures reached
  # main that way, each costing a merge and a rebuild to learn something a pull
  # request could have said. A pull request now composes and verifies; it does
  # not publish and it does not deploy.
  workflow_run:
    workflows: [ci]
    types: [completed]
  # `workflow_run` does not fire for a run that was itself triggered by
  # `pull_request` -- GitHub declines to let pull-request code start a
  # default-branch workflow, which is the right call and also means the
  # composition cannot be reached that way. Removing the branch filter above
  # was therefore necessary and not sufficient; this is the trigger that fires.
  #
  # It starts as soon as the pull request does, so it can outrun the host image
  # it needs. "Resolve the host image" waits for that image rather than
  # assuming it, which is a wait measured against the same commit both
  # workflows agree on.
  pull_request:
    branches: [main]
  # Kept for an extension that is given a credential to announce itself, and
  # for a hand-sent signal. Nothing sends this today -- which is exactly why
  # the schedule below exists, and why this listener alone was worse than
  # useless: it made the pipeline look wired while an extension merge quietly
  # reached nothing.
  repository_dispatch:
    types: [extension-admitted]
  # An extension's own merge cannot trigger this: sending a signal from one
  # repository to another needs a credential, and giving a private repository a
  # long-lived token that can start builds in a public one is a worse trade
  # than waiting a few minutes. So the host asks instead of being told.
  #
  # A scheduled run rebuilds nothing unless the composition's inputs actually
  # changed -- see "Decide whether anything changed" -- so this costs one cheap
  # resolution per tick and deploys only when there is something to deploy. It
  # also self-heals: any trigger that is ever missed is picked up by the next
  # tick rather than waiting for somebody to notice.
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:
    inputs:
      reason:
        description: Why this composition is being rebuilt
        required: false
        default: manual rebuild

permissions:
  contents: read
  packages: write
  # Read-only, and only so "Resolve the host image" can ask whether the host
  # build it is waiting for has already failed.
  actions: read

concurrency:
  # Never two compositions at once *for the same target*: they would race for
  # the same image tag and the deploy that finished second would silently win.
  # Keyed by the branch being composed, so a pull request's composition neither
  # queues behind main's nor delays it -- they build different tags and only
  # one of them can deploy.
  group: composition-${{ github.event.workflow_run.head_branch || github.ref_name }}
  cancel-in-progress: false

env:
  # Whether this composition is entitled to publish and deploy. A pull request
  # composes and verifies and stops there; only main, an extension's admission
  # dispatch, or a hand-run rebuild goes further. Decided once here rather than
  # re-derived at each step, because the two places that must agree about it
  # are the publish step and the deploy job.
  IS_RELEASE: ${{ (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main') || github.event_name == 'repository_dispatch' || github.event_name == 'workflow_dispatch' || github.event_name == 'schedule' }}
  CORDON_COMMIT: a5f2f2d2c11e33abe1be6ab04e5961f3c2f4a739
  PYTHONUNBUFFERED: "1"

jobs:
  compose:
    name: Compose admitted extensions
    # A failed host build must not be composed onto. And a fork's pull request
    # is not composed at all: reading the extensions' admission artifacts needs
    # a secret a fork cannot have, so it would fail on the download step with a
    # permissions error that looks like a broken pipeline rather than a fork
    # doing exactly what forks are supposed to do.
    if: >-
      (github.event_name != 'workflow_run' ||
       github.event.workflow_run.conclusion == 'success') &&
      (github.event_name != 'pull_request' ||
       github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.publish.outputs.image }}
    steps:
      - name: Check out host
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Read the composition set
        # A secret, and never interpolated into the script body. Actions
        # expands `${{ … }}` into the text of the step it echoes, and masks only
        # secret values -- so anything private reaches the log through the
        # environment or not at all.
        env:
          COMPOSITION_EXTENSIONS: ${{ secrets.COMPOSITION_EXTENSIONS }}
        run: |
          set -euo pipefail
          if [ -z "${COMPOSITION_EXTENSIONS:-}" ]; then
            echo "::error::COMPOSITION_EXTENSIONS must be set as a repository" \
                 "secret. A repository variable cannot hold it: this log is" \
                 "public and only a secret is masked."
            exit 1
          fi
          printf '%s' "$COMPOSITION_EXTENSIONS" > composition-set.json
          if ! jq -e '.extensions | length > 0' composition-set.json >/dev/null 2>&1; then
            echo "::error::COMPOSITION_EXTENSIONS must list at least one extension"; exit 1
          fi
          jq -r '.extensions | length as $n | "composing \($n) extension(s)"' composition-set.json

      - name: Mask the extension names for the rest of the job
        # This step prints a count and nothing else, which was enough while it
        # was the only step that mentioned them. It is not: later steps shell
        # out to gh, pip, and Django, and those echo repository names, wheel
        # filenames, app labels and — when a test fails — table and column
        # names. This repository is public and so are its logs.
        #
        # Masking once here covers every later step, including output from
        # commands nobody has written yet. Auditing each one forever is the
        # alternative, and it already failed: a query-budget assertion printed
        # the composed schema into a world-readable log.
        #
        # Every spelling, because the same extension appears as a repository
        # path, a bare name, a Python package, a table prefix and the path it
        # mounts at. A URL is none of the other four, so it needs its own.
        run: |
          set -euo pipefail
          jq -r '
            .extensions[]
            | .id, .repository,
              (.repository | split("/") | last),
              (.id | split(".") | last)
          ' composition-set.json \
            | tr -d '\r' | sed '/^$/d' | sort -u \
            | while read -r name; do
                echo "::add-mask::${name}"
                echo "::add-mask::${name//-/_}"
                echo "::add-mask::${name//_/-}"
              done

      - name: Authenticate to GHCR
        # Before the two steps that ask the registry a question, not before the
        # one that pushes: resolving the host image and checking whether this
        # composition was already published are both reads, and both happen on
        # every tick including the ones that build nothing.
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve the host image
        id: host
        env:
          GH_TOKEN: ${{ github.token }}
        # Derived from the commit being composed, so there is no pin to bump by
        # hand and no way for the composition to be built on a stale host.
        run: |
          set -euo pipefail
          ref="${{ github.event.workflow_run.head_sha || github.event.pull_request.head.sha || github.event.client_payload.host_sha || github.sha }}"
          image="ghcr.io/${{ github.repository }}:sha-${ref:0:12}"
          # On a pull request this workflow and the host build start together,
          # so the image may not exist yet. Waited for rather than assumed:
          # failing instantly here would report "no such image" for a build that
          # is simply still running, which is the least useful true statement
          # available. 20 minutes is well past a host build and well short of
          # the job timeout.
          digest=""
          # Five seconds, not twenty. On a pull request this is waiting for a
          # build that started at the same moment, so the granularity of the
          # poll is pure overshoot -- on average half the interval, spent after
          # the image is already there.
          for attempt in $(seq 1 240); do
            if digest=$(docker buildx imagetools inspect "$image" \
                          --format '{{json .Manifest.Digest}}' 2>/dev/null | tr -d '"'); then
              [ -n "$digest" ] && break
            fi
            if [ "$attempt" -eq 1 ]; then
              echo "waiting for the host image $image to be published..."
            fi
            # Every two minutes, ask whether there is still a build to wait for.
            # A host build that already failed will never publish, so waiting
            # the full twenty minutes reports a timeout instead of the failure
            # that caused it -- and holds this branch's composition slot while
            # the corrected commit queues behind it.
            if [ $((attempt % 24)) -eq 0 ]; then
              host_ci=$(gh api \
                "repos/$GITHUB_REPOSITORY/actions/runs?head_sha=$ref&per_page=30" \
                --jq '[.workflow_runs[] | select(.name == "ci") | .conclusion]
                      | map(select(. != null)) | first' 2>/dev/null || true)
              case "$host_ci" in
                failure|cancelled|timed_out)
                  echo "::error title=Host build $host_ci::The host build for \
          ${ref:0:12} concluded '$host_ci', so $image will never be published. \
          Fix the host build; the composition has nothing to compose onto."
                  exit 1
                  ;;
              esac
            fi
            sleep 5
          done
          if [ -z "$digest" ]; then
            echo "::error title=Host image never appeared::$image was not published \
          within 20 minutes. The host build for this commit failed or was skipped; \
          the composition has nothing to compose onto."
            exit 1
          fi
          echo "image=${image%:*}@$digest" >> "$GITHUB_OUTPUT"
          echo "host image: ${image%:*}@$digest"

      - name: Find the latest admission for each extension
        env:
          # Fine-grained token with read access to the private extension repos'
          # Actions artifacts. The extensions stay private; only their signed
          # admission bundles and wheels are read.
          GH_TOKEN: ${{ secrets.EXTENSION_ARTIFACTS_TOKEN }}
        # One list call per extension and nothing downloaded. This is the whole
        # input to "has anything changed?", and it is deliberately the cheapest
        # question that can answer it: a new wheel cannot exist without a new
        # admission run, so an unchanged set of run ids means an unchanged set
        # of wheels. It can be conservative in one direction only -- a re-run
        # that produced an identical wheel looks like a change and rebuilds --
        # which is the safe direction to be wrong in.
        #
        # Written to a file rather than a step output. An output is read back by
        # interpolation, which puts its contents into the text of the step that
        # reads it, and these lines name the private repositories.
        run: |
          set -euo pipefail
          : > admissions.txt
          while read -r repo; do
            run=$(gh run list --repo "$repo" --workflow admit-plugin.yml \
                    --branch main --status success --limit 1 \
                    --json databaseId -q '.[0].databaseId')
            if [ -z "$run" ]; then
              echo "::error::an extension has no successful admission on main"; exit 1
            fi
            printf '%s=%s\n' "$repo" "$run" >> admissions.txt
          done < <(jq -r '.extensions[].repository' composition-set.json | sort)
          wc -l < admissions.txt | xargs printf 'resolved %s admission(s)\n'

      - name: Decide whether anything changed
        id: fingerprint
        run: |
          set -euo pipefail
          # What a composition IS: this host image, these admitted wheels, this
          # policy. Two runs agreeing on all three produce the same image, so
          # there is nothing to build and nothing to deploy.
          #
          # Asked here, before anything is downloaded or verified, because the
          # answer is almost always "nothing". Asked after, an idle tick still
          # fetched three wheels, installed a verifier and checked three
          # signatures against Sigstore -- tens of times a day, to learn that.
          # The cost was not the minutes; it was that every one of those was a
          # chance to fail at something nobody was waiting for.
          fingerprint=$(printf '%s\n%s\n%s\n' \
            "${{ steps.host.outputs.image }}" \
            "$(sha256sum admissions.txt | cut -d' ' -f1)" \
            "$(openssl dgst -sha256 policy/plugin-admission-v1.json | awk '{print $NF}')" \
            | sha256sum | cut -c1-32)
          echo "fingerprint=$fingerprint" >> "$GITHUB_OUTPUT"

          tag="ghcr.io/${{ github.repository }}/composition:fp-$fingerprint"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

          # Recorded as a tag rather than a file or a variable: the registry
          # already holds the only copy of the truth that matters, and a tag
          # that exists is proof the exact same composition was published.
          if docker buildx imagetools inspect "$tag" >/dev/null 2>&1; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "composition fp-$fingerprint is already published."
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "composition fp-$fingerprint has not been built before."
          fi

      - name: Nothing to do
        # Only a scheduled run stops here. Every other trigger rebuilds even
        # when the inputs match, because a hand-run rebuild asking for exactly
        # that is the one case where "nothing changed" is not an answer.
        if: >-
          github.event_name == 'schedule' &&
          steps.fingerprint.outputs.changed == 'false'
        run: |
          {
            echo "### Composition already current"
            echo
            echo "Host image, extension wheels and admission policy are unchanged"
            echo "since \`fp-${{ steps.fingerprint.outputs.fingerprint }}\` was published."
            echo "Nothing was built and nothing was deployed."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Download admitted artifacts
        env:
          # Fine-grained token with read access to the private extension repos'
          # Actions artifacts. The extensions stay private; only their signed
          # admission bundles and wheels are read.
          GH_TOKEN: ${{ secrets.EXTENSION_ARTIFACTS_TOKEN }}
        # The run ids were resolved above to answer "anything changed?", so
        # they are reused rather than looked up a second time.
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        run: |
          set -euo pipefail
          mkdir -p artifacts
          while IFS='=' read -r repo run; do
            [ -n "$repo" ] || continue
            gh run download "$run" --repo "$repo" -D "artifacts/$(basename "$repo")"
          done < admissions.txt

      - name: Check out exact Cordon verifier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        with:
          repository: joeseverino/cordon
          ref: ${{ env.CORDON_COMMIT }}
          path: cordon

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'

      - name: Install pinned Cordon dependencies
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        run: npm ci --prefix cordon --ignore-scripts

      - name: Verify every admission against the declared signer
        id: collect
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        # The host verifies each signature itself rather than trusting an entry
        # an extension produced. The identity checked is the one declared in
        # composition-set.json, so an extension cannot widen who is
        # allowed to sign for it by editing its own repository.
        run: |
          set -euo pipefail
          policy_sha=$(openssl dgst -sha256 policy/plugin-admission-v1.json | awk '{print $NF}')
          args=""
          count=$(jq '.extensions | length' composition-set.json)
          for index in $(seq 0 $((count - 1))); do
            repo=$(jq -r ".extensions[$index].repository" composition-set.json)
            workflow=$(jq -r ".extensions[$index].workflow" composition-set.json)
            plugin_id=$(jq -r ".extensions[$index].id" composition-set.json)
            dir="artifacts/$(basename "$repo")"
            statement=$(find "$dir" -name admission.json -print -quit)
            bundle=$(find "$dir" -name admission.sigstore.json -print -quit)
            wheel=$(find "$dir" -name '*.whl' -print -quit)
            if [ -z "$statement" ] || [ -z "$bundle" ] || [ -z "$wheel" ]; then
              echo "::error::$repo is missing its admission statement, bundle or wheel"; exit 1
            fi
            entry="$dir/verified-entry.json"
            node cordon/admission/verify.mjs \
              --statement "$statement" \
              --bundle "$bundle" \
              --artifact "$wheel" \
              --identity "https://github.com/$repo/$workflow@refs/heads/main" \
              --issuer https://token.actions.githubusercontent.com \
              --repository "$repo" \
              --workflow "$workflow" \
              --host severino-hq \
              --plugin-id "$plugin_id" \
              --policy-sha "$policy_sha" > "$entry"
            args="$args --entry $entry --wheel $wheel"
          done
          echo "args=$args" >> "$GITHUB_OUTPUT"

      - name: Merge locks and stage the build context
        id: compose
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        env:
          CORDON_LOCK: ./cordon/admission/lock.mjs
        run: python scripts/compose-plugins.py ${{ steps.collect.outputs.args }} --out build/composition

      - name: Build composed image
        # Skipped only by a scheduled run that found nothing to do.
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        env:
          HQ_IMAGE: ${{ steps.host.outputs.image }}
        run: |
          docker build -f composition/Dockerfile \
            --build-arg HQ_IMAGE="$HQ_IMAGE" \
            --build-arg PLUGIN_WHEEL_DIGESTS="${{ steps.compose.outputs.digests }}" \
            --build-arg PLUGIN_POLICY_SHA256="${{ steps.compose.outputs.policy_sha256 }}" \
            --build-arg SEVERINO_HQ_PLUGIN_REFERENCES="${{ steps.compose.outputs.references }}" \
            -t ghcr.io/${{ github.repository }}/composition:${{ github.sha }} .

      - name: Verify the composition as one application
        # Skipped only by a scheduled run that found nothing to do.
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        # Per-plugin admission proves each wheel against the host in isolation.
        # This second layer is deliberately after assembly: only the real image
        # can expose duplicate routes/capabilities, migration conflicts, missing
        # host-owned dependencies, or a test that wrongly assumes its plugin is
        # the only contributor to a global surface.
        #
        # Log level raised for the run. At INFO the application writes a line
        # per request, which a suite exercising every composed page turns into
        # a list of everything the composition serves. Masking is the backstop;
        # not emitting it is the fix. Failures are unaffected -- the test runner
        # reports those itself.
        run: |
          image=ghcr.io/${{ github.repository }}/composition:${{ github.sha }}
          docker run --rm --entrypoint python \
            --env DJANGO_SECRET_KEY=ci-only-composition-key-0123456789abcdef0123456789abcdef \
            --env DJANGO_ALLOWED_HOSTS=localhost \
            --env SEVERINO_LOG_LEVEL=WARNING \
            "$image" manage.py check
          docker run --rm --entrypoint python \
            --env DJANGO_SECRET_KEY=ci-only-composition-key-0123456789abcdef0123456789abcdef \
            --env DJANGO_ALLOWED_HOSTS=localhost \
            --env SEVERINO_LOG_LEVEL=WARNING \
            "$image" manage.py test --verbosity 1

      - name: Scan composed image
        # Skipped only by a scheduled run that found nothing to do.
        if: >-
          github.event_name != 'schedule' ||
          steps.fingerprint.outputs.changed == 'true'
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: ghcr.io/${{ github.repository }}/composition:${{ github.sha }}
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: "1"

      - name: Authenticate to GHCR
        if: >-
          env.IS_RELEASE == 'true' &&
          (github.event_name != 'schedule' ||
           steps.fingerprint.outputs.changed == 'true')
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # A pull request has already had the only thing it was here for: the
      # image was built and its suite was run inside it. Publishing is what
      # makes an image deployable, so a pull request does not.
      - name: Publish by digest
        if: >-
          env.IS_RELEASE == 'true' &&
          (github.event_name != 'schedule' ||
           steps.fingerprint.outputs.changed == 'true')
        id: publish
        run: |
          image=ghcr.io/${{ github.repository }}/composition:${{ github.sha }}
          docker push "$image"
          # The fingerprint tag is what the next scheduled run reads to decide
          # there is nothing to do. Pushed after the image itself, so a failed
          # push never leaves a claim that this composition was published.
          docker tag "$image" "${{ steps.fingerprint.outputs.tag }}"
          docker push "${{ steps.fingerprint.outputs.tag }}"
          digest=$(docker buildx imagetools inspect "$image" --format '{{json .Manifest.Digest}}' | tr -d '"')
          echo "image=${image%:*}@$digest" >> "$GITHUB_OUTPUT"

  deploy:
    name: Deploy composition
    needs: compose
    # Belt and braces with the publish step above: that one produces nothing
    # for a pull request, and this one refuses to run for it at all. Either
    # alone would do; both, because a self-hosted runner executing code from an
    # unmerged branch is the failure you do not get to fix afterwards.
    # Spelled out rather than reading the IS_RELEASE env above: a job-level
    # `if` cannot see the `env` context. Same condition, and the pair are kept
    # adjacent in review by both carrying this note.
    # `needs.compose.outputs.image` carries the whole answer to "was anything
    # published?": it is empty for a pull request, and empty for a scheduled
    # run that found the composition already current. Requiring it here means
    # this job cannot be handed an empty image to deploy, whatever the trigger.
    if: >-
      needs.compose.result == 'success' &&
      needs.compose.outputs.image != '' &&
      ((github.event_name == 'workflow_run' &&
        github.event.workflow_run.head_branch == 'main') ||
       github.event_name == 'repository_dispatch' ||
       github.event_name == 'workflow_dispatch' ||
       github.event_name == 'schedule')
    # Self-hosted runner ON the homelab. It dials out to GitHub; GitHub never
    # connects in. NEVER trigger a self-hosted job from pull_request.
    runs-on: [self-hosted, severino-hq]
    environment: production
    permissions:
      contents: read
      packages: read
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    steps:
      - name: Log in to GHCR
        # The same pinned action the composing job uses, rather than a hand
        # rolled `docker login`. It logs out again when the job ends, which
        # matters here and nowhere else: this runner is a machine in the house,
        # not a container thrown away afterwards, and `docker login` writes the
        # credential to a file in the runner's home directory and leaves it
        # there. Every deploy was topping up a registry token on the homelab
        # that nothing ever removed.
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Sync deploy checkout
        run: git -C /opt/apps/severino-hq pull --ff-only
      - name: Deploy exact image with health rollback
        # Same deploy path and rollback as a host release: it pulls, recreates,
        # waits for health, activates the controller, and restores the previous
        # image if any step fails.
        run: /opt/apps/severino-hq/scripts/deploy-image.sh "${{ needs.compose.outputs.image }}"
      - name: Reclaim disk
        if: always()
        run: |
          docker image prune -af --filter "until=24h" || true
          docker builder prune -af || true
