Django>=6.1.1,<6.2
mcp>=1.30.0,<2
mozilla-django-oidc>=5.0.2,<6.0
uvicorn>=0.53.0,<1
whitenoise>=6.12.0
# Extension wheels install with --no-deps, so every library an extension
# imports is pinned here or the composed image does not have it.
defusedxml>=0.7,<0.8
# Both already resolved transitively; declared directly because an extension
# imports them and a transitive pin can disappear when its parent changes.
cryptography>=50.0.1,<51
httpx>=0.28,<1
# Lets a failing test in a parallel worker report its real traceback instead of
# "cannot pickle 'traceback' object". Here rather than in a test-only file
# because the composed image runs the suite in parallel as its own admission
# gate, and that gate's failures have to be readable too.
tblib>=3,<4
