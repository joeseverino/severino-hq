flowchart LR
    operator[Operator]
    vault[(Obsidian vault)]
    mcp[severino-vault-mcp]
    hq[Severino HQ]
    ci["CI: lint, test, security, scan"]
    ghcr[(GHCR image)]
    runner["self-hosted runner (homelab)"]

    operator -->|edit frontmatter| vault
    operator -->|hq sync| mcp -->|manifest| cli["hq MCP wrapper"] -->|one atomic capability| hq
    operator -->|git push or hq ship| ci -->|on green| ghcr --> runner -->|pull and restart| hq

    %% the destination every path converges on
    classDef anchor stroke-width:2px;
    class hq anchor;
