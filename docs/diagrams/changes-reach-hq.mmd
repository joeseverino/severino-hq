flowchart LR
    joe[Joe]
    vault[(Obsidian vault)]
    mcp[severino-vault-mcp]
    hq[Severino HQ]
    ci["CI: lint, test, security, scan"]
    ghcr[(GHCR image)]
    runner["self-hosted runner (homelab)"]

    joe -->|edit frontmatter| vault
    joe -->|hq sync| mcp -->|manifest + topology| cli["hq MCP wrapper"] -->|one atomic capability| hq
    joe -->|git push or hq ship| ci -->|on green| ghcr --> runner -->|pull and restart| hq

    %% the destination every path converges on — styled by .anchor in the shared theme
    classDef anchor stroke-width:2px;
    class hq anchor;
