flowchart TB
    subgraph declared["One provider, declared once"]
        direction LR
        model["Provider definition<br/>one pydantic model<br/>+ one declaration"]
        says["What it says about itself<br/>fields, bounds, defaults, facet,<br/>hostnames, seeding, live records"]
        model --> says
    end

    subgraph derived["Derived from it, no other file edited"]
        direction LR
        schema["JSON Schema"]
        validate["Validation"]
        contract["Controller contract"]
        forms["Forms"]
        service["Service view"]
        adopt["Adoption"]
        schema ~~~ validate ~~~ contract
        forms ~~~ service ~~~ adopt
    end

    declared --> derived

    classDef anchor stroke-width:2px;
    class model,says anchor;
